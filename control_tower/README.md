# Control Tower moved

This folder is no longer the Control Tower app.

The standalone app lives at:

`apps/control-tower` (`@worxstream/control-tower`)

Run it from the platform repo root:

```bash
npm run dev:control-tower
```

UI: `http://localhost:5175`
